import React from 'react';

import './FavoritesButton.css';

const FavoritesButton = () => {
  return (
    <button
      className="FavoritesButton button-filled"
      type="button"
    >Избранное</button>
  );
};

export default FavoritesButton;