import React from 'react';

import HeaderButton from './HeaderButton';

const SearchButton = () => {
  return (
    <HeaderButton
      cls="SearchButton"
      icon="&#xf002;"
    >Поиск</HeaderButton>
  );
};

export default SearchButton;