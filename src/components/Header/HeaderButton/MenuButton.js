import React from 'react';

import HeaderButton from './HeaderButton';

const MenuButton = () => {
  return (
    <HeaderButton
      cls="MenuButton"
      icon="&#xf0c9;"
    >Меню</HeaderButton>
  );
};

export default MenuButton;